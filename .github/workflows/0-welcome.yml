name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
jobs:
  build: # <--- Este es el nombre del job que se espera en este paso
    name: Post welcome comment
    runs-on: ubuntu-latest
