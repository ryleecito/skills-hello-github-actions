name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
jobs:
  post-welcome-comment: # <--- Change 'build' to the expected name
    name: Post welcome comment
    runs-on: ubuntu-latest

      # You will likely add steps here later in the exercise
